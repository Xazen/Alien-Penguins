using UnityEngine;
using System.Collections;

public class MichaImportantNotes : MonoBehaviour {

	//spawner must be child object on environmant/plane/spaceship (whatever is moved via leap)
	//the spawner should be a above the ground approx the height of 1 astronaut (to avoid collision while spawning)

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
